/**
 * TRUST v2 — Design token barrel export
 *
 * Import tokens from here:
 *   import { brand, decision, spacing, textScale, radius } from '@/lib/tokens';
 */

export { brand, neutral, decision, pillar, band } from './colors';
export { spacing } from './spacing';
export type { SpacingKey } from './spacing';
export { fontFamily, textScale } from './typography';
export type { TextVariant } from './typography';
export { radius } from './radius';
export type { RadiusKey } from './radius';
