/**
 * Placeholder test — validates the test infrastructure is wired correctly.
 * Replace with real tests as TRUST v2 farmer dashboard screens are built.
 */
describe("Test infrastructure", () => {
  it("jest runs in dashboard-farmer", () => {
    expect(true).toBe(true);
  });
});
