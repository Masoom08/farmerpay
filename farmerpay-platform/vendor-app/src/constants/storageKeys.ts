export const STORAGE_KEYS = {
  USER: "vendor_user",
} as const;

export const SECURE_STORAGE_KEYS = {
  ACCESS_TOKEN: "vendor_access_token",
  REFRESH_TOKEN: "vendor_refresh_token",
  MPIN_ENABLED: "vendor_mpin_enabled",
  BIOMETRIC_ENABLED: "vendor_biometric_enabled",
} as const;